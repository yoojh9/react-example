import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App'; // js 확장자는 생략해도 됨

const rootElement = document.getElementById('root');
ReactDOM.render(<App/>, rootElement);
